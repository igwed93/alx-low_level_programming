#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - prints the length of a given string
 * @s: the string argument whose length is to be printed
 *
 * Return: the length of s
 */
int _strlen(char *s)
{
	return (strlen(s));
}
