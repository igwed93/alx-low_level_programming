#include <stdio.h>

/**
 * sub - subtracts an int from another int,
 * and returns the result
 * @a: integer
 * @b: integer to subtract from a.
 *
 * Return: a - b
 */
int sub(int a, int b)
{
	return (a - b);
}
