#include <stdio.h>
#include <stdlib.h>

/**
 * main - program that prints the base file
 * that it was compiled from
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
	printf("%s\n", __BASE_FILE__);
	return (O);
}
