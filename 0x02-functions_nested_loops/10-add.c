#include "main.h"

/**
 * add - computes the sum of two integers and returns the result
 * @a: integer to be added to b
 * @b: initeger to be added to a
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}

